name: opensearch_firewall
priority: 50
transformations:
  - id: field_name_mapping
    type: field_name_mapping
    mapping:
      src_ip: source.ip
      dst_ip: destination.ip
      src_port: source.port
      dst_port: destination.port
      action: event.action
