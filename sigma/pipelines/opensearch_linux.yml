name: opensearch_linux
priority: 50
transformations:
  - id: field_name_mapping
    type: field_name_mapping
    mapping:
      program: process.name
      user: user.name
      src_ip: source.ip
      hostname: host.name
